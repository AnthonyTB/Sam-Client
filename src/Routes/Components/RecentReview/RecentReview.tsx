import React, { useEffect, useContext } from 'react';
import './RecentReview.css';

const RecentReview: React.FC<any> = (props) => {
  return <section className={props.type}></section>;
};

export default RecentReview;
